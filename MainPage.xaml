<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SkiaSharp_TrimWarnings_3._116._0.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:SkiaSharp.Extended.UI.Controls;assembly=SkiaSharp.Extended.UI"
    Title="SkiaSharp Warnings">

    <VerticalStackLayout
        HorizontalOptions="Center"
        VerticalOptions="Center">

        <controls:SKLottieView
            HeightRequest="300"
            WidthRequest="300"
            x:Name="LottieView"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            Source="Lottie/dotnetbot.json"
            IsAnimationEnabled="True"
            RepeatCount="-1"/>
        
            <!-- AnimationCompleted="OnAnimationCompleted" -->
            <!-- AnimationFailed="OnAnimationFailed" -->
            <!-- AnimationLoaded="OnAnimationLoaded" -->
            <!-- IsAnimationEnabled="True" /> -->
        <!--  IsAnimationEnabled="True" />  -->
        <!--  Progress="{Binding Progress}"  -->
        <!--  Duration="{Binding Duration}" />  -->

        <!--  <Image  -->
        <!--  Source="dotnet_bot.png"  -->
        <!--  Aspect="AspectFit"  -->
        <!--  HeightRequest="185"  -->
        <!--  SemanticProperties.Description="dot net bot in a hovercraft number nine" />  -->
    </VerticalStackLayout>

</ContentPage>
